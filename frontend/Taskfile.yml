# https://taskfile.dev

version: '3'

tasks:
  default: pnpm start
  setup: pnpm i
  check: pnpm run biome check --write .
  api: pnpm run openapi-ts
  build: pnpm run ng build --configuration production
