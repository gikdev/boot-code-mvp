# https://taskfile.dev
version: '3'

tasks:
  default: dotnet run --project ./src/Backend.Api/
  dev: dotnet watch run --project ./src/Backend.Api/
  build: dotnet build --no-restore
  restore: dotnet restore ./Backend.slnx
